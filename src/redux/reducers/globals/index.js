/*
 * Globals
 */
import {GENERIC_ERROR_MSG} from '../../../utils/constants/constants';

export const isFetching = false;
export const fetchSuccess = false;
export const fetchError = false;
export const networkError = false;
export const error = GENERIC_ERROR_MSG;
